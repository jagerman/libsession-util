# One directory per example: each adds its own executable and keeps its sources, config and
# documentation together.  Every example target is EXCLUDE_FROM_ALL, so these cost nothing until
# someone asks for one by name.
add_subdirectory(echo-bot)
